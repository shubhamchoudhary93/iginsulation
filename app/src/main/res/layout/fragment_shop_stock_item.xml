<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="shopStockTransaction"
            type="com.shubham.iginsulation.database.shopStockTransaction.ShopStockTransaction" />

        <variable
            name="clickListener"
            type="com.shubham.iginsulation.shopStock.ShopStockAdaptor.ShopStockTransactionListener" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:onClick="@{() -> clickListener.onClick(shopStockTransaction)}">

        <TextView
            android:id="@+id/name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:nameString="@{shopStockTransaction}" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=" - " />

        <TextView
            android:id="@+id/quantity"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:quantityString="@{shopStockTransaction}" />
    </LinearLayout>
</layout>